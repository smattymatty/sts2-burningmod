// All FireOrb patches now handled by BaseLib's CustomOrbModel:
// - CreateSprite → CustomOrbModel.CreateCustomSprite()
// - Icon → CustomOrbModel.CustomIconPath
// - GetRandomOrb → CustomOrbModel.IncludeInRandomPool
